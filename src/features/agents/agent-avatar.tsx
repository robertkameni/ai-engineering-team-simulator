/**
 * Re-export shared avatar from components (arch-review Sprint A).
 * Non-workspace features must import from `@/components/agents/agent-avatar`.
 */
export { AgentAvatar } from "@/components/agents/agent-avatar";
