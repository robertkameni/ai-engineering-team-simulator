/**
 * Contract: `/api/simulate` awaits core artifact synthesis before SSE `done`
 * (aligned with AGENTS.md). Polling remains a client safety net only.
 */
export const SIMULATION_AWAITS_ARTIFACT_SYNTHESIS_BEFORE_DONE = true;
